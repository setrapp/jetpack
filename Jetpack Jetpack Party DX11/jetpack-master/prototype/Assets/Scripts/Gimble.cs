using UnityEngine;
using System.Collections;

public class Gimble : MonoBehaviour {
	void Update() {
		/*TODO: Make the gimble always point up and stay orthogonal to player camera*/
		//Vector3 target = transform.position + Vector3.forward;
		//transform.LookAt(target, transform.up);
	}
}
